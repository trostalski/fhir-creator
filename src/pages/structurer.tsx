import Layout from "@/components/Layout";
import StructurerBody from "@/components/structurer/StructurerBody";

const Structurer = () => {
  return (
    <Layout>
      <StructurerBody />
    </Layout>
  );
};

export default Structurer;
