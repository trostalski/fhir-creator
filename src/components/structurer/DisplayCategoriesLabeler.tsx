import { DisplayCategoriesLabelerProps } from "@/types";

const DisplayCategoriesLabeler = (props: DisplayCategoriesLabelerProps) => {
  const {
    selectedCategories,
    setSelectedCategories,
    colors,
    setColors,
    getColor,
    focusedCategory,
    setFocusedCategory,
  } = props;

  return <div>LabelerDisplay</div>;
};

export default DisplayCategoriesLabeler;
